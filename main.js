const name = prompt("Ismingizni kiriting:");

const spaceId = prompt("Space ID ni kiriting:");

const spacePassword = prompt("Space Password ni kiriting:");

const oka = {
    name: name,
    id: spaceId,
    password: spacePassword
};

console.log(oka);
